import React from 'react'
import ReactDOM from 'react-dom/client'
import { MaticadDBApp } from './MaticadDBApp.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MaticadDBApp />
  </React.StrictMode>,
)
